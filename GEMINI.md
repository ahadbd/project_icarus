Create a Python script called script.py that acts as a health reminder.

It must run a loop that triggers a tkinter popup every 30 minutes.

When the user clicks 'OK' on the popup, it must append the current date and time to a file named stretch_log.csv.

Also, create a second script dashboard.py using matplotlib that reads stretch_log.csv and displays a bar chart of 'Stretches per Day'.

Ensure the code is clean, commented, and handles the case where the CSV file doesn't exist yet.
